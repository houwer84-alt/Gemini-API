# 📦 Gemini WebAPI 本地开发完整指南

> **当前状态**: 已为你准备好所有开发文件和配置！

## 🎯 目标

成功将项目克隆到本地，安装依赖，并进行二次开发。

---

## 📋 开发环境要求

✅ **Python**: 3.10 或更高（当前系统：3.13.3）  
✅ **Git**: 用于克隆项目  
✅ **网络**: 能访问 GitHub 和 PyPI  
⚠️ **可选**: 安装 `make` 工具（简化命令）

---

## 🚀 完整步骤

### 第 1 步：克隆项目

```bash
# 方法 1: 克隆官方仓库
git clone https://github.com/HanaokaYuzu/Gemini-API.git
cd Gemini-API

# 方法 2: 如果你已经 Fork 了项目
git clone https://github.com/你的用户名/Gemini-API.git
cd Gemini-API
```

### 第 2 步：创建虚拟环境（强烈推荐）

```bash
# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
# Linux/macOS:
source venv/bin/activate

# Windows (CMD):
venv\Scripts\activate.bat

# Windows (PowerShell):
venv\Scripts\Activate.ps1

# 验证虚拟环境已激活（命令提示符前应显示 (venv)）
which python  # Linux/macOS
where python  # Windows
```

### 第 3 步：安装依赖

#### 方式 A：使用 Makefile（推荐）

```bash
# 安装所有开发依赖
make dev-install

# 或只安装基础依赖
make install
```

#### 方式 B：手动安装

```bash
# 1. 以可编辑模式安装项目
pip install -e .

# 2. 安装开发工具
pip install black flake8 pytest pytest-asyncio mypy

# 3. 可选：安装浏览器 cookie 支持
pip install browser-cookie3
```

### 第 4 步：配置认证

#### 方法 1：使用环境变量（推荐）

```bash
# 复制环境变量模板
cp .env.example .env

# 编辑 .env 文件
nano .env  # 或使用你喜欢的编辑器
```

在 `.env` 文件中填入：

```bash
SECURE_1PSID=你的__Secure-1PSID值
SECURE_1PSIDTS=你的__Secure-1PSIDTS值
```

**获取 Cookies 步骤**：
1. 访问 https://gemini.google.com 并登录
2. 按 `F12` 打开开发者工具
3. 切换到 `Application` 标签（Chrome）
4. 左侧找到 `Cookies` → `https://gemini.google.com`
5. 复制 `__Secure-1PSID` 和 `__Secure-1PSIDTS` 的值

#### 方法 2：使用 browser-cookie3

如果安装了 `browser-cookie3`，只需确保浏览器已登录 Gemini，代码会自动读取。

### 第 5 步：验证安装

```bash
# 使用 Makefile
make verify

# 或手动运行
python3 test_installation.py
```

**预期输出**：
```
✅ GeminiClient, ChatSession 导入成功
✅ 常量模块导入成功
✅ 类型模块导入成功
✅ 异常类导入成功
✅ 日志模块导入成功
...
🎉 所有测试通过！环境配置成功！
```

### 第 6 步：运行开发测试

```bash
# 使用 Makefile
make dev-test

# 或手动运行
python3 dev_example.py
```

这会运行实际的 API 测试（需要有效的 cookies）。

---

## 🛠️ 开发工作流

### 日常开发流程

```bash
# 1. 激活虚拟环境
source venv/bin/activate

# 2. 拉取最新代码
git pull origin master

# 3. 进行你的修改...
# 编辑 src/gemini_webapi/ 中的文件

# 4. 格式化代码
make format

# 5. 运行代码检查
make lint

# 6. 运行测试
make test

# 7. 提交更改
git add .
git commit -m "feat: 你的功能描述"
git push
```

### 添加新功能示例

假设你要添加一个新方法来获取对话历史：

**1. 编辑 `src/gemini_webapi/client.py`**:

```python
class GeminiClient(GemMixin):
    # ... 现有代码 ...
    
    @running(retry=2)
    async def get_chat_history(self, chat_id: str) -> list:
        """
        获取对话历史
        
        Parameters
        ----------
        chat_id: str
            对话ID
            
        Returns
        -------
        list
            对话历史记录
        """
        response = await self._batch_execute([
            RPCData(
                rpcid=GRPC.READ_CHAT,
                payload=json.dumps([chat_id]).decode()
            )
        ])
        
        # 解析响应...
        return parsed_history
```

**2. 编写测试 `tests/test_chat_history.py`**:

```python
import unittest
from gemini_webapi import GeminiClient

class TestChatHistory(unittest.IsolatedAsyncioTestCase):
    async def test_get_chat_history(self):
        client = GeminiClient()
        await client.init()
        
        # 测试你的新方法
        history = await client.get_chat_history("test_id")
        self.assertIsInstance(history, list)
```

**3. 运行测试**:

```bash
pytest tests/test_chat_history.py -v
```

---

## 📁 项目文件说明

我已经为你创建了以下开发辅助文件：

### ✅ 已创建的文件

| 文件名 | 用途 |
|--------|------|
| `test_installation.py` | 验证安装是否成功 |
| `dev_example.py` | 开发环境快速测试脚本 |
| `.env.example` | 环境变量模板 |
| `DEVELOPMENT.md` | 详细的开发指南 |
| `QUICKSTART_DEV.md` | 快速开始指南 |
| `Makefile` | 常用命令快捷方式 |
| `.gitignore` | Git 忽略文件配置 |
| `本地开发完整指南.md` | 本文档 |

### 📂 核心源码目录

```
src/gemini_webapi/
├── __init__.py          # 包入口
├── client.py            # ⭐ 主客户端类（最常修改）
├── constants.py         # ⭐ 常量定义（端点、模型）
├── exceptions.py        # 异常类
│
├── components/
│   └── gem_mixin.py    # Gem 功能
│
├── types/              # 数据类型
│   ├── candidate.py
│   ├── gem.py
│   ├── grpc.py
│   ├── image.py
│   └── modeloutput.py
│
└── utils/              # 工具函数
    ├── decorators.py
    ├── get_access_token.py
    ├── logger.py
    └── ...
```

---

## 🔧 常用开发命令

```bash
# 查看所有可用命令
make help

# 安装依赖
make install          # 基础依赖
make dev-install      # 开发依赖

# 代码质量
make format           # 格式化代码
make lint             # 代码检查
make type-check       # 类型检查

# 测试
make test             # 运行测试
make test-cov         # 测试 + 覆盖率
make verify           # 验证安装
make dev-test         # 开发测试

# 清理
make clean            # 清理临时文件

# 构建
make build            # 构建包

# 全部检查
make all              # 格式化 + 检查 + 测试
```

---

## 🐛 调试技巧

### 1. 启用详细日志

```python
from gemini_webapi import set_log_level

set_log_level("DEBUG")
```

### 2. 查看原始 API 响应

在 `client.py` 中添加：

```python
response = await self.client.post(...)
print("原始响应:", response.text)  # 调试时添加
```

### 3. 使用断点调试

```python
# 在需要调试的地方添加
breakpoint()

# 或使用 pdb
import pdb
pdb.set_trace()
```

### 4. 抓包分析 API

1. 打开 Chrome DevTools (F12)
2. 切换到 Network 标签
3. 过滤 XHR/Fetch 请求
4. 在 Gemini 网页中执行操作
5. 查看 `StreamGenerate` 等请求
6. 分析请求/响应结构

---

## 📚 学习资源

### 项目文档
- `README.md` - 使用说明
- `DEVELOPMENT.md` - 开发指南（详细）
- `QUICKSTART_DEV.md` - 快速开始

### 代码示例
- `tests/` - 单元测试示例
- `dev_example.py` - 快速测试脚本

### 外部资源
- [Python asyncio 文档](https://docs.python.org/3/library/asyncio.html)
- [Pydantic 文档](https://docs.pydantic.dev/)
- [HTTPX 文档](https://www.python-httpx.org/)

---

## ❓ 常见问题

### Q1: 安装依赖时报错？

```bash
# 升级 pip
pip install --upgrade pip setuptools wheel

# 然后重新安装
pip install -e .
```

### Q2: 导入模块失败？

确保：
1. ✅ 虚拟环境已激活
2. ✅ 运行了 `pip install -e .`
3. ✅ 在项目根目录下

### Q3: 测试失败 "No module named 'gemini_webapi'"？

```bash
# 重新安装
pip install -e .

# 验证安装
python -c "import gemini_webapi; print('OK')"
```

### Q4: cookies 无效或过期？

1. 重新从浏览器获取最新的 cookies
2. 确保浏览器仍然登录
3. 或使用 `browser-cookie3` 自动读取

### Q5: 如何使用代理？

在 `.env` 文件中添加：

```bash
HTTP_PROXY=http://127.0.0.1:7890
HTTPS_PROXY=http://127.0.0.1:7890
```

或在代码中：

```python
client = GeminiClient(
    secure_1psid="...",
    proxy="http://127.0.0.1:7890"
)
```

---

## 🎯 下一步建议

### 初学者
1. ✅ 完成上述安装步骤
2. 📖 阅读 `README.md` 了解基本用法
3. 🧪 运行 `dev_example.py` 测试功能
4. 📝 查看 `tests/` 目录学习代码

### 中级开发者
1. 📖 仔细阅读 `DEVELOPMENT.md`
2. 🔍 研究 `client.py` 的实现
3. 🧪 尝试添加新功能
4. 🐛 在 GitHub Issues 中找问题修复

### 高级开发者
1. 🔬 抓包分析 Gemini API 结构
2. 💡 实现复杂功能（如流式输出）
3. 📊 优化性能
4. 🤝 贡献代码到主仓库

---

## 📞 获取帮助

- 📖 查看项目文档
- 🐛 [GitHub Issues](https://github.com/HanaokaYuzu/Gemini-API/issues)
- 💬 GitHub Discussions
- 📧 联系维护者

---

## ✅ 检查清单

在开始开发前，确保：

- [ ] Python 版本 >= 3.10
- [ ] 已克隆项目到本地
- [ ] 已创建并激活虚拟环境
- [ ] 已运行 `pip install -e .`
- [ ] 所有依赖安装成功
- [ ] `test_installation.py` 测试通过
- [ ] 已配置 cookies（`.env` 或 browser-cookie3）
- [ ] `dev_example.py` 运行成功
- [ ] 阅读了 `DEVELOPMENT.md`

---

**🎉 恭喜！你已经准备好开始二次开发了！**

祝你开发愉快！如果遇到问题，请参考上述文档或提交 Issue。

---

*最后更新: 2025-10-22*
